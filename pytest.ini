[pytest]
markers =
    integration: marks tests requiring external services like NATS (deselect with '-m "not integration"')
# By default, skip integration tests unless explicitly requested
addopts = -m "not integration"
